name: '👀 Auto assign'

on:
  workflow_call:

jobs:
  add-reviews:
    name: Add reviews
    runs-on: ubuntu-latest
    steps:
      - name: Assign
        uses: kentaro-m/auto-assign-action@v1.2.0
