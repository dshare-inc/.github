name: ✅ Check (Gradle)

on:
  workflow_call:
    inputs:
      java_version:
        type: string
        description: 'Java version (default is 11)'
        default: '11'
        required: false
      java_distribution:
        type: string
        description: 'Java distribution (default is adopt)'
        default: 'adopt'
        required: false
      commands:
        type: string
        description: 'Gradle test command. split by ",". build and code coverage report is @.'
        default: 'test jacocoTestReport @ build -x test @ build/reports/jacoco/report.xml'
        required: true
      clean_build:
        type: boolean
        description: 'Build after clean'
        default: true
        required: false
      codecov_env:
        type: string
        description: 'Codecov.io environment (phase)'
        default: 'test'
        required: false
    secrets:
      AWS_ROLE_REGION:
        required: true
      AWS_CODE_MANAGER_ROLE:
        required: true
      AWS_ROLE_SESSION_DURATION:
        required: true
      AWS_CODEARTIFACT_REGION:
        required: true
      AWS_CODEARTIFACT_DOMAIN:
        required: true
      AWS_CODEARTIFACT_OWNER:
        required: true
      CODECOV_REPO_TOKEN:
        required: true
      SLACK_BOT_TOKEN:
        required: true
      SLACK_REVIEW_NOTIFICATION_CHANNEL:
        required: true

env:
  JAVA_VERSION: ${{ inputs.java_version == null && '11' || inputs.java_version }}
  JAVA_DISTRIBUTION: ${{ inputs.java_distribution == null && 'adopt' || inputs.java_distribution }}
  CLEAN_BUILD: ${{ inputs.clean_build == null && true || inputs.clean_build }}
  CODECOV_ENV: ${{ inputs.codecov_env == null && 'test' || inputs.codecov_env }}

concurrency:
  group: check-${{ github.repository }}-${{ github.ref }}-${{ github.event_name }}
  cancel-in-progress: true

jobs:
  Matrix:
    name: 'Matrix'
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.extract.outputs.result }}
    steps:
      - name: Extract
        uses: actions/github-script@v5
        id: extract
        env:
          MATRIX: ${{ inputs.commands }}
        with:
          result-encoding: string
          script: |
            const { MATRIX } = process.env;

            console.log(`${MATRIX} <- Input`);

            if (MATRIX == null || MATRIX == '') {
              throw Error('Undefined matrix');
            }

            return JSON.stringify(MATRIX.split(',')
              .map(item => item.trim())
              .map(item => {
                const result = item.split('@');
                return {
                  test: result[0].trim(),
                  build: result[1].trim(),
                  report: result[2].trim()
                }
              })
            )

  Tests:
    name: 'Tests'
    if: ${{ github.event_name != 'pull_request' || (github.event_name == 'pull_request' && github.event.action != 'unlabeled' && !contains(github.event.pull_request.labels.*.name, 'hotfix 🚨')) || (github.event_name == 'pull_request' && github.event.action == 'unlabeled' && github.event.label.name == 'hotfix 🚨') }}
    needs:
      - Matrix
    strategy:
      matrix:
        gradle: ${{ fromJson(needs.Matrix.outputs.matrix) }}
    runs-on: actions-runner-system
    timeout-minutes: 16
    outputs:
      status: ${{ job.status }}
    steps:
      - name: Find Comment
        uses: peter-evans/find-comment@v1
        id: fc
        if: ${{ github.event_name == 'pull_request' && !contains(github.event.pull_request.labels.*.name, 'hotfix 🚨') }}
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body-includes: 'by dshare-bot, PR COMMENT (check-gradle) ${{ matrix.gradle.test }}'
          direction: last

      - name: Create PR Comment
        if: ${{ github.event_name == 'pull_request' && !contains(github.event.pull_request.labels.*.name, 'hotfix 🚨') && steps.fc.outputs.comment-id == '' }}
        uses: peter-evans/create-or-update-comment@v1
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ### 🏃 테스트 `${{ matrix.gradle.test }}` 를 진행중입니다!
            지금 `PR-${{ github.event.pull_request.number }}` 에 대해 테스트를 진행하고 있습니다. 조금만 기다려주세요!
            테스트 결과는 PR Comment, Slack 으로 확인할 수 있습니다.

            by dshare-bot, PR COMMENT (check-gradle) ${{ matrix.gradle.test }}

      - name: Update PR Comment
        if: ${{ github.event_name == 'pull_request' && !contains(github.event.pull_request.labels.*.name, 'hotfix 🚨') && steps.fc.outputs.comment-id != '' }}
        uses: peter-evans/create-or-update-comment@v1
        with:
          comment-id: ${{ steps.fc.outputs.comment-id }}
          edit-mode: replace
          body: |
            ### 🏃 다시 테스트 `${{ matrix.gradle.test }}` 를 진행중입니다! 😧
            지금 `PR-${{ github.event.pull_request.number}}` 에 대해 테스트를 진행하고 있습니다. 조금만 기다려주세요!
            테스트 결과는 PR Comment, Slack 으로 확인할 수 있습니다.

            by dshare-bot, PR COMMENT (check-gradle) ${{ matrix.gradle.test }}

      - name: Checkout Pull Request VCS
        uses: actions/checkout@v2
        if: ${{ github.event_name == 'pull_request' }}
        with:
          token: ${{ github.token }}
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Checkout VCS (Non PR)
        uses: actions/checkout@v2
        if: ${{ github.event_name != 'pull_request' }}
        with:
          token: ${{ github.token }}
          ref: ${{ github.ref }}

      - name: Set up JDK
        uses: actions/setup-java@v2
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DISTRIBUTION }}
          architecture: x64
          cache: gradle

      - name: Cache
        uses: actions/cache@v2
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: AWS AssumeRole (Auth)
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-region: ${{ secrets.AWS_ROLE_REGION }}
          role-to-assume: ${{ secrets.AWS_CODE_MANAGER_ROLE }}
          role-duration-seconds: ${{ secrets.AWS_ROLE_SESSION_DURATION }}

      - name: AWS CodeArtifact Authentication (Gradle)
        uses: dshare-inc/actions-codeartifact-auth@1.0.1
        with:
          region: ${{ secrets.AWS_CODEARTIFACT_REGION }}
          domain: ${{ secrets.AWS_CODEARTIFACT_DOMAIN }}
          owner: ${{ secrets.AWS_CODEARTIFACT_OWNER }}
          type: gradle
          repo: gradle

      - name: Clean
        if: ${{ env.CLEAN_BUILD == 'true' }}
        id: clean
        run: |
          CLEAN_RESULT="$(./gradlew --console verbose clean -Dconsole.encoding=UTF-8 -Dfile.encoding=UTF-8 --no-daemon --parallel)"
          echo "$CLEAN_RESULT"

          CLEAN_RESULT="${CLEAN_RESULT//'%'/'%25'}"
          CLEAN_RESULT="${CLEAN_RESULT//$'\n'/'%0A'}"
          CLEAN_RESULT="${CLEAN_RESULT//$'\r'/'%0D'}"
          CLEAN_RESULT="$(echo $CLEAN_RESULT | sed 's/\x1B\[[0-9;]\+[A-Za-z]//g')"
          echo "::set-output name=clean_result::$(echo $CLEAN_RESULT)"

      - name: Build
        id: build
        run: |
          BUILD_RESULT="$(./gradlew --console verbose build ${{ matrix.gradle.build }} -Dconsole.encoding=UTF-8 -Dfile.encoding=UTF-8 --no-daemon --parallel)"
          echo "$BUILD_RESULT"

          BUILD_RESULT="${BUILD_RESULT//'%'/'%25'}"
          BUILD_RESULT="${BUILD_RESULT//$'\n'/'%0A'}"
          BUILD_RESULT="${BUILD_RESULT//$'\r'/'%0D'}"
          BUILD_RESULT="$(echo $BUILD_RESULT | sed 's/\x1B\[[0-9;]\+[A-Za-z]//g')"
          echo "::set-output name=build_result::$(echo $BUILD_RESULT)"

      - name: Tests
        if: ${{ always() }}
        id: test
        continue-on-error: true
        run: |
          TEST_RESULT="$(./gradlew --console verbose ${{ matrix.gradle.test }} -Dconsole.encoding=UTF-8 -Dfile.encoding=UTF-8 --no-daemon --parallel)"
          echo "$TEST_RESULT"

          TEST_RESULT="${TEST_RESULT//'%'/'%25'}"
          TEST_RESULT="${TEST_RESULT//$'\n'/'%0A'}"
          TEST_RESULT="${TEST_RESULT//$'\r'/'%0D'}"
          TEST_RESULT="$(echo $TEST_RESULT | sed 's/\x1B\[[0-9;]\+[A-Za-z]//g')"
          echo "::set-output name=test_result::$(echo $TEST_RESULT)"

    - name: Upload code coverage report
      uses: actions/upload-artifact@v2
      continue-on-error: true
      with:
        name: ${{ matrix.gradle.test }}-report
        path: ${{ matrix.gradle.report }}
        retention-days: 21
        if-no-files-found: error

    - name: Add Code Coverage Report
      uses: codecov/codecov-action@v2
      if: ${{ always() }}
      continue-on-error: true
      with:
        token: ${{ secrets.CODECOV_REPO_TOKEN }}
        flags: ${{ matrix.gradle.test }}
        name: ${{ matrix.gradle.test }}
        files: ${{ matrix.gradle.report }}

      - name: Find Comment (After)
        uses: peter-evans/find-comment@v1
        id: fc_after
        if: ${{ github.event_name == 'pull_request' && !contains(github.event.pull_request.labels.*.name, 'hotfix 🚨') }}
        continue-on-error: true
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body-includes: 'by dshare-bot, PR COMMENT (check-gradle) ${{ matrix.gradle.test }}'
          direction: last

      - name: Succeed (Create PR Comment)
        if: ${{ github.event_name == 'pull_request' && steps.test.outcome == 'success' && steps.fc_after.outputs.comment-id == '' }}
        continue-on-error: true
        uses: peter-evans/create-or-update-comment@v1
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ### ✅ 테스트 `${{ matrix.gradle.test }}` 에 성공했습니다!
            **🎉 현재 시스템의 테스트가 모두 통과하였습니다.** 자세한 내역은 Action 의 `Details` 버튼을 누른 후 확인하실 수 있습니다.

            ### 🧹 Clean
            ```
            ${{ steps.clean.outputs.clean_result }}
            ```

            ### 💾 Build
            ```
            ${{ steps.build.outputs.build_result }}
            ```

            ### 🤖 Tests
            ```
            ${{ steps.test.outputs.test_result }}
            ```

            by dshare-bot, PR COMMENT (check-gradle) ${{ matrix.gradle.test }}

      - name: Succeed (Update PR Comment)
        if: ${{ github.event_name == 'pull_request' && steps.test.outcome == 'success' && steps.fc_after.outputs.comment-id != '' }}
        continue-on-error: true
        uses: peter-evans/create-or-update-comment@v1
        with:
          comment-id: ${{ steps.fc.outputs.comment-id }}
          edit-mode: replace
          body: |
            ### ✅ 테스트 `${{ matrix.gradle.test }}` 에 성공했습니다!
            **🎉 현재 시스템의 테스트가 모두 통과하였습니다.** 자세한 내역은 Action 의 `Details` 버튼을 누른 후 확인하실 수 있습니다.

            ### 🧹 Clean
            ```
            ${{ steps.clean.outputs.clean_result }}
            ```

            ### 💾 Build
            ```
            ${{ steps.build.outputs.build_result }}
            ```

            ### 🤖 Tests
            ```
            ${{ steps.test.outputs.test_result }}
            ```

            by dshare-bot, PR COMMENT (check-gradle) ${{ matrix.gradle.test }}

      - name: Failed (Create PR Comment)
        if: ${{ github.event_name == 'pull_request' && steps.test.outcome != 'success' && steps.fc_after.outputs.comment-id == '' }}
        continue-on-error: true
        uses: peter-evans/create-or-update-comment@v1
        with:
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            ### ❌ 테스트에 `${{ matrix.gradle.test }}` 실패하였습니다!
            현재 시스템의 테스트가 실패하였습니다. 아래 Action의 `Details` 를 확인하시거나, Comment를 통해 제공된 Console Log를 참고하시기 바랍니다.

            ### 🧹 Clean
            ```
            ${{ steps.clean.outputs.clean_result }}
            ```

            ### 💾 Build
            ```
            ${{ steps.build.outputs.build_result }}
            ```

            ### 🤖 Tests
            ```
            ${{ steps.test.outputs.test_result }}
            ```

            by dshare-bot, PR COMMENT (check-gradle) ${{ matrix.gradle.test }}

      - name: Failed (Update PR Comment)
        if: ${{ github.event_name == 'pull_request' && steps.test.outcome != 'success' && steps.fc_after.outputs.comment-id != '' }}
        continue-on-error: true
        uses: peter-evans/create-or-update-comment@v1
        with:
          comment-id: ${{ steps.fc.outputs.comment-id }}
          edit-mode: replace
          body: |
            ### ❌ 테스트에 `${{ matrix.gradle.test }}` 실패하였습니다!
            현재 시스템의 테스트가 실패하였습니다. 아래 Action의 `Details` 를 확인하시거나, Comment를 통해 제공된 Console Log를 참고하시기 바랍니다.

            ### 🧹 Clean
            ```
            ${{ steps.clean.outputs.clean_result }}
            ```

            ### 💾 Build
            ```
            ${{ steps.build.outputs.build_result }}
            ```

            ### 🤖 Tests
            ```
            ${{ steps.test.outputs.test_result }}
            ```

            by dshare-bot, PR COMMENT (check-gradle) ${{ matrix.gradle.test }}

  NotifyResult:
    needs:
      - Tests
    if: ${{ github.event_name != 'pull_request' || (github.event_name == 'pull_request' && !contains(github.event.pull_request.labels.*.name, 'hotfix 🚨')) }}
    name: 'Notification (Result)'
    runs-on: ubuntu-latest
    steps:
      - name: Succeed (Slack PR)
        uses: slackapi/slack-github-action@v1.16.0
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
        with:
          channel-id: ${{ secrets.SLACK_REVIEW_NOTIFICATION_CHANNEL }}
          payload: |
            {
            	"blocks": [
            		{
            			"type": "section",
            			"text": {
            				"type": "mrkdwn",
            				"text": "*${{ needs.Tests.result == 'success' && '✅ 테스트 및 검증 성공* _(SUCCESS)_' || '❌ 테스트 및 검증 실패* _(FAILURE)' }}\n_브랜치 또는 Pull request 검증(테스트, Lint, Code coverage)에 성공하였습니다._"
            			}
            		},
            		{
            			"type": "section",
            			"fields": [
            				{
            					"type": "mrkdwn",
            					"text": "> *Repository:*\n> ${{ github.repository }}"
            				},
            				{
            					"type": "mrkdwn",
            					"text": "> *${{ github.event_name == 'pull_request' && 'PR Number' || 'Branch' }}:*\n> ${{ github.event_name == 'pull_request' && github.event.pull_request.number || github.ref }}"
            				},
            				{
            					"type": "mrkdwn",
            					"text": "> *${{ github.event_name == 'pull_request' && 'PR Title' || 'Commit' }}:*\n> ${{ github.event_name == 'pull_request' && github.event.pull_request.title || github.sha }}"
            				}
            			]
            		},
            		{
            			"type": "actions",
            			"elements": [
            				{
            					"type": "button",
            					"text": {
            						"type": "plain_text",
            						"emoji": true,
            						"text": "📄 ${{ github.event_name == 'pull_request' && 'PR로' || 'Commit으로' }} 바로가기"
            					},
            					"value": "go_to_pr",
            					"url": "${{ github.event_name == 'pull_request' && format('{0}/{1}/pull/{2}', github.server_url, github.repository, github.event.pull_request.number) || format('{0}/{1}/commit/{2}', github.server_url, github.repository, github.sha) }}"
            				},
            				{
            					"type": "button",
            					"text": {
            						"type": "plain_text",
            						"emoji": true,
            						"text": "🚀 Action 결과로 바로가기"
            					},
            					"value": "go_to_action",
            					"url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
            				}
            			]
            		},
            		{
            			"type": "context",
            			"elements": [
            				{
            					"type": "mrkdwn",
            					"text": "*${{ github.actor }}* 님이 실행 또는 해당 브랜치(PR)를 작업하셨습니다."
            				}
            			]
            		}
            	]
            }
