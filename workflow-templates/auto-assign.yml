name: '👀 Auto assign'

on:
  pull_request:
    types:
      - opened
      - reopened
      - ready_for_review

jobs:
  workflow:
    uses: dshare-inc/.github/.github/workflows/auto-assign.yml@main
